# Builder Pattern

## Simple Meaning

Builder creates an object step by step, especially when the object has many optional values.

## When To Use

Use it when constructors become long or confusing.

## Example

The example builds a `Computer` with required and optional fields.


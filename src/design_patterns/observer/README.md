# Observer Pattern

## Simple Meaning

Observer notifies many objects when one object changes.

## When To Use

Use it for events, subscriptions, notifications, and UI updates.

## Example

The example notifies subscribers when a channel uploads a video.


# Factory Method Pattern

## Simple Meaning

Factory Method creates objects without making the calling code know the exact class.

## When To Use

Use it when code asks for a general type, but the program decides the real object.

## Example

The example creates different `Notification` objects based on a string like `email` or `sms`.


# Iterator Pattern

## Simple Meaning

Iterator gives a standard way to visit items one by one.

## When To Use

Use it when client code should not know how a collection stores its data.

## Example

The example uses Java's built-in `Iterator` to read names from a custom collection.


# Bridge Pattern

## Simple Meaning

Bridge separates what something does from how it does it.

## When To Use

Use it when both the abstraction and implementation can change independently.

## Example

The example separates a `Remote` from the `Device` it controls.


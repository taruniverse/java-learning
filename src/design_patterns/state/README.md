# State Pattern

## Simple Meaning

State changes an object's behavior when its internal state changes.

## When To Use

Use it when many `if` statements depend on the object's current state.

## Example

The example changes a traffic light from red to green to yellow.


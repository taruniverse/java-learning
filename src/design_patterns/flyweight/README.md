# Flyweight Pattern

## Simple Meaning

Flyweight reuses shared objects instead of creating many duplicate objects.

## When To Use

Use it when many objects have the same internal data.

## Example

The example reuses the same `TreeType` object for many trees with the same name and color.


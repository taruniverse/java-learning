# Adapter Pattern

## Simple Meaning

Adapter makes an existing class work with a different interface.

## When To Use

Use it when you cannot change an old class, but new code expects a different method.

## Example

The example adapts an old charger so it can be used as a modern USB-C charger.


# Facade Pattern

## Simple Meaning

Facade gives a simple method for a group of complicated steps.

## When To Use

Use it when client code should not know many internal classes.

## Example

The example uses a `HomeTheaterFacade` to start multiple devices with one method.


# Visitor Pattern

## Simple Meaning

Visitor adds new operations to classes without changing those classes.

## When To Use

Use it when you have stable object classes but often add new operations.

## Example

The example calculates shipping for different item types using a visitor.


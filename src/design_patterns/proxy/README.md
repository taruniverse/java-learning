# Proxy Pattern

## Simple Meaning

Proxy controls access to another object.

## When To Use

Use it for lazy loading, access checks, logging, or caching.

## Example

The example loads an image only when `display()` is called.


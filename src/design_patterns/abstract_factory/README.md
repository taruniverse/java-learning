# Abstract Factory Pattern

## Simple Meaning

Abstract Factory creates families of related objects without naming their exact classes.

## When To Use

Use it when objects should be created together, like light theme buttons with light theme checkboxes.

## Example

The example creates UI components from either a light theme factory or a dark theme factory.


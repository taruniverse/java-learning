# Prototype Pattern

## Simple Meaning

Prototype creates a new object by copying an existing object.

## When To Use

Use it when object creation is expensive or when you want a similar object with small changes.

## Example

The example copies a `GameCharacter` and then changes only the copied character's name.


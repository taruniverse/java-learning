# Decorator Pattern

## Simple Meaning

Decorator adds extra behavior to an object without changing its class.

## When To Use

Use it when you want flexible combinations of features.

## Example

The example starts with plain coffee and wraps it with milk and sugar decorators.


# Mediator Pattern

## Simple Meaning

Mediator makes objects talk through one central object instead of talking directly.

## When To Use

Use it when many objects communicate and the connections become messy.

## Example

The example uses a chat room mediator between users.


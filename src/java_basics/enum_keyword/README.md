# Enum Keyword

## Simple Meaning

`enum` is used when a variable should have one value from a fixed list.

## When To Use

Use it for days, directions, order status, user roles, and similar fixed choices.

## Example

`OrderStatus` can be only `PLACED`, `SHIPPED`, or `DELIVERED`.


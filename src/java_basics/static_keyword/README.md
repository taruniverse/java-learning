# Static Keyword

## Simple Meaning

`static` belongs to the class, not to one object.

## Common Uses

- Static variable: shared by all objects.
- Static method: called using class name.

## Example

`Counter.count` is shared by all `Counter` objects.


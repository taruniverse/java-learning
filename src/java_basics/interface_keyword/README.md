# Interface Keyword

## Simple Meaning

An interface is a contract. It tells what methods a class must have.

## Keyword

Use `implements`.

## Example

`Printer` is an interface. `InkPrinter` implements it.


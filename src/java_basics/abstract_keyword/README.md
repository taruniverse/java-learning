# Abstract Keyword

## Simple Meaning

`abstract` means incomplete.

## Important Points

- An abstract class cannot be directly created using `new`.
- An abstract method has no body.
- A child class must complete abstract methods.

## Example

`Shape` has abstract method `area()`. `Circle` completes it.


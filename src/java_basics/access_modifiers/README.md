# Access Modifiers

## Simple Meaning

Access modifiers control where variables and methods can be used.

## Types

- `public`: accessible everywhere.
- `private`: accessible only inside the same class.
- `protected`: accessible in same package and child classes.
- default: no keyword, accessible inside the same package.

## Example

The example shows public methods accessing private data safely.


# Encapsulation

## Simple Meaning

Encapsulation means keeping data private and allowing access through methods.

## Why It Helps

It protects data from invalid changes.

## Example

`BankAccount` keeps `balance` private and changes it only through `deposit()`.


# Constructor

## Simple Meaning

A constructor runs when an object is created.

## Important Points

- It has the same name as the class.
- It has no return type.
- It can set starting values.

## Example

`Book` sets its title and author inside the constructor.


# Polymorphism

## Simple Meaning

Polymorphism means one method name can have different behavior.

## Two Common Types

- Compile-time polymorphism: method overloading.
- Runtime polymorphism: method overriding.

## Example

`Animal animal = new Dog()` calls the dog version of `sound()`.


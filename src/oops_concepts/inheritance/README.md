# Inheritance

## Simple Meaning

Inheritance means a child class gets properties or methods from a parent class.

## Keyword

Use `extends`.

## Example

`Car` extends `Vehicle`, so it can use `start()`.


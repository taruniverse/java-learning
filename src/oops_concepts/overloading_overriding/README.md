# Overloading And Overriding

## Overloading

Same method name, different parameters.

## Overriding

Child class changes the parent class method.

## Example

`Calculator` overloads `add()`.
`Bike` overrides `start()`.


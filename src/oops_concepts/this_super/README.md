# This And Super

## `this`

`this` means the current object.

## `super`

`super` means the parent class.

## Example

`Employee` uses `this.name` for its own field and `super(name)` to call the parent constructor.

